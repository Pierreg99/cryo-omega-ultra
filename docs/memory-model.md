# Memory model

**Status (0.3.x): not implemented.** Working / Episodic / Semantic / Procedural / User-Project memory namespaces are planned for **0.4+** (ADR candidate after Ops foundation).

Do not invent on-disk memory schemas in 0.3.x. Agent conversation context today is request-scoped only (messages passed to `/api/chat`).
